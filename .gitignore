node_modules/
site/

# Entirely disposable: the SQLite database is rebuilt from history/ on demand.
# The committed price record lives in history/, deliberately outside this dir.
data/

*.db
*.db-shm
*.db-wal
.env
# NOT ignored: the Pages workflow runs on a fresh checkout, so hand-entered
# prices must be committed for CI to see them. It holds prices, not secrets.
.DS_Store
npm-debug.log*
